# Intervention Search Results

Found: {{ count }} intervention{% if count != 1 %}s{% endif %}
{% if query %}Query: {{ query }}{% endif %}

{% if count == 0 -%}
No interventions found matching the filters.
{% else -%}
|ID|Name|Type|Category|Synonyms|
|---|---|---|---|---|
{% for r in results -%}
|{{ r.id or "-" }}|{{ r.name }}|{{ r.intervention_type or "-" }}|{{ r.category or "-" }}|{% if r.synonyms %}{{ r.synonyms | join(", ") | truncate(45) }}{% else %}-{% endif %}|
{% endfor %}

Requires `NCI_API_KEY`.
Filters: -q <name>, --type <type>, --category <category>, --code <code>
{% if pagination_footer %}

{{ pagination_footer }}
{% endif %}
{% endif -%}
