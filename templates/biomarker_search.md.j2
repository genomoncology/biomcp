# Biomarker Search Results

Found: {{ count }} biomarker{% if count != 1 %}s{% endif %}
{% if query %}Query: {{ query }}{% endif %}

{% if count == 0 -%}
No biomarkers found matching the filters.
{% else -%}
|ID|Name|Gene|Type|Assay|Trials|
|---|---|---|---|---|---|
{% for r in results -%}
|{{ r.id or "-" }}|{{ r.name }}|{{ r.gene or "-" }}|{{ r.biomarker_type or "-" }}|{{ r.assay_type or "-" }}|{{ r.trial_count or "-" }}|
{% endfor %}

Requires `NCI_API_KEY`.
Filters: -q <name>, --type <type>, --eligibility <text>, --assay-purpose <text>, --code <code>
{% if pagination_footer %}

{{ pagination_footer }}
{% endif %}
{% endif -%}
