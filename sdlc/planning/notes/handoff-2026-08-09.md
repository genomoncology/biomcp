# Handoff — 2026-08-09 (factory operator session)

Written at full context before compaction. Sole operator: Ian stopped the
biomcp-team session ("they might have been messing"); this session owns
queue, sdlc, botassembly operations AND biomcp triage. Monitoring runs as
a harness Monitor task (b1o05gqnz) polling the queue event table read-only
every 30s — if it dies, monitoring is dark, not quiet.

## The day's landings, all DEPLOYED (pulls done ~16:32Z, suites green)

- queue 0028: consecutive environment refusals pause the channel. The
  landed code resets the counter AFTER successful prepare
  (dispatch.ts:133), not on claim — the design stage fixed my ticket's
  wrong wording on its own.
- sdlc 0015: queue contract pins in sdlc's suite (82 tests + 8 specs).
  These pins ARE the deploy gate: pull queue → run sdlc suite → green
  means live. First real exercise passed.
- sdlc 0016: per-repo tick failures stream the moment they settle.
- sdlc 0017: two-hour stage caps on 03-code and 04-code-review (ACTIVE —
  assembly checkout pulled). First flight refused correctly: bot seals
  the assembly from the operator's CHECKOUT, not origin/main — every
  sdlc landing is inert until the operator pulls. Ticket corrected.
- sdlc 0018: script ceiling (prepare/integrate/discard) 15→30 min.
  biomcp's warm prepare took 14.8 min; cold doesn't fit 15.
- Deploy order that works: pull sdlc first, then queue; suites must be
  green BETWEEN the pull and the next cron tick (symlinks make pulls
  live instantly). Schema v3 unchanged today — no rebuild needed.

## Adversarial review (brief at planning/feedback/stack-review-brief-2026-08-09.md in workspace)

Findings filed and flying:
- queue 0029 (dispatch scope ≠ concurrency channel; prefix-match claim
  steals across channels) — in backoff after two WebSocket drops.
- queue 0030 (exit 2 + published run id is a fault, not a refusal —
  explains every "bot refused ... stderr=fault: WebSocket closed 1006"
  event today) — not yet claimed.
- sdlc 0019 (worktree identity + ownership proof before deletion) —
  re-flying after a correct code-stage refusal; ticket now authorizes
  restating tests/scripts.test.ts:196 through design.
- botassembly 0008 (capture no-follow on EVERY path component; the
  leaf-only fix was probed and beaten via parent-dir symlink swap) —
  re-flying with both blockers baked into the ticket.

Issues filed (need design/Ian): sdlc main-is-pushed-before-its-green-proof
(IAN RULING), sdlc expired-claim-not-recoverable, sdlc lifecycle-scripts-
diverged (no repo has the union; biomcp discard lacks retry tag), sdlc
stack-contract-harness (build AFTER 0029/0030/0019 land), queue
rebuild-two-realities, botassembly credential-boundary (IAN RULING:
workspace BOT_AUTH law vs bot ADR 0017 XDG credentials; scrub+allowlist
recommended either way).

Review mediums not filed as files — tracked in the review doc only.

## biomcp triage — DONE except Ian's domain ruling

Channel PAUSED ("backlog triage per 2026-08-09 adversarial review...").
- 7 duplicates archived with record mapping (commit 4311424a); spot-checked
  0886→0647 and 0887→0640/0641 before trusting the rest.
- 6 corrections (commit f68b67f2): 0682 phantom dep "665"→landed 0683;
  0690+0673 machine-readable deps: added; 0877 narrowed (0679 landed the
  refusal; only typed error remains); 0884 narrowed (fixture landed; only
  fail-closed network ratchet remains); 0880 split → NEW 0894; 0885
  inventory rebuilt from SPEC_LIVE_PATHS (0632/0633 landed, clingen-cspec
  item obsolete; owns article-assets/graph + variant-myvariant/articles
  live canaries; classify stray ddinter-live.md).
- 0686: blocked, 3 attempts spent. Amended (c08a7d11) binding next attempt
  to the design review's SIGKILL findings (seven fixtures leak; pidfd-style
  owner identity; six red owner-death tests exist). BEFORE teardown: tag
  the claim branch tip — repair commit 1cc32958 lives there and biomcp's
  discard lacks the retry tag. Check whether re-ready resets attempts.
- AWAITING IAN: ruling on 8 domain-gated tickets (0688 0689 0874 0878
  0879 0881 0882 0883 — builds, gnomAD, ClinGen, full-text). Options
  offered: review himself / name a domain reviewer / fly with domain
  review at design stage. On ruling: resume channel; the 13 clean +
  corrected tickets fly regardless.

## Operational patterns proven today

- Blocked-ticket re-fly: fix ticket on origin/main (scratch worktree via
  `git worktree add <scratch> origin/main` + push HEAD:main if checkout
  is behind), then `git worktree remove --force worktrees/<repo>/<id>`,
  `git branch -D ticket/<id>`, `git push origin :refs/heads/ticket/<id>`;
  next sync re-readies. Used for 0017, 0008, 0019.
- THREE refusals today = tickets not authorizing updates to tests that
  pin changed behavior. TODO not yet done: add "which existing tests pin
  this behavior, and does the ticket authorize restating them?" to the
  ticket skill (repos/sdlc/skills/ticket/SKILL.md).
- Provider (openai-codex) dropped WebSocket 1006 at ~05:05, 14:37,
  16:22×2, 16:40. Refund+backoff machinery absorbs each cleanly. These
  log as "bot refused" but are faults — 0030's defect, live.
- Queue DB writes are safe during ticks now (0027 busy timeout deployed).
  Reads: node -e with {readOnly:true}.
- sdlc pause needs -m. `echo ===` breaks the harness shell. biomcp
  pre-commit compiles Rust ~22s.

## Ian decisions pending (in one place)

1. Domain owner for the 8 biomcp clinical tickets (blocks channel resume).
2. Credential boundary: BOT_AUTH law vs ADR 0017 (botassembly issue).
3. Landing-order redesign: push-after-sealed-gates (sdlc issue).
4. Cargo package: 148 biomedical testdata files — legal/domain question.

## Immediate next steps

1. Watch 0019, 0008 settle; 0029 re-claims 16:55; 0030 after it. Dissect
   any refusal; expect test-authorization gaps first.
2. After 0029/0030 land: deploy window (pull queue+sdlc, suites, resume).
   Then build the contract harness ticket.
3. On Ian's ruling: resume biomcp; re-fly 0686 (tag branch first).
4. Add the test-pinning question to the ticket skill.
