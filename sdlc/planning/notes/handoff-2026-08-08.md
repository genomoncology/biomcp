# Handoff — 2026-08-08

Read, act, then delete this file in your first commit.

BioMCP's migration off March onto sdlc. All four prep jobs are now done.
What is left is verification, not construction.

## State

- **THE CHANNEL IS PAUSED — this is the only fence now.** Ian's
  instruction (2026-08-08, evening): the team is still cleaning up
  tickets; nothing here flies until he says go. With the quarantine
  cause fixed, the next sync imports 35 ready tickets, and only the
  pause (`genomoncology/biomcp`) holds them. Do not resume without
  Ian's word. A queue database rebuild drops pauses — re-pause
  immediately after any rebuild.
- **Registered on beelink.** The laptop's `~/.local/share/sdlc/repos` does
  not list this repo — a local registry check is misleading, do not repeat
  that mistake.
- **The archive id collisions are fixed and the quarantine cause is gone.**
  `sdlc/tickets/archive/` holds 557 entries with no duplicate ids;
  `sdlc/records/` holds 477. Regenerated by
  `~/workspace/repos/sdlc/scripts/march-harvest.py`, never hand-patched.
- **Backlog is in place and `sdlc/issues/` is empty.** 35 live tickets,
  1 draft (0499), 0 issues. Every finding is a ticket; nothing waits in
  a folder. `ls sdlc/tickets` is the current answer — do not trust counts
  written here.
- **Gate ladder is green on main**, run from `$HOME` as the dispatcher
  would: `lint` 0, `test` 0 (451 passed, ~9.5 min), `spec` 0. That is
  `prepare`'s precondition satisfied.

## Just decided

- **Numbering stays on the slug, not the `id` field.** The harvester issue
  asked for the id; numbering biomcp from the id produces six collisions
  against the slug's four, because the three disagreeing ids (48, 49, 59)
  are themselves already taken by other tickets. Where a number really was
  issued twice, the ticket whose own id matches the number keeps it and the
  later completion steps up by 800 — so 0873 is the misfiled repo-cleanup
  ticket and 0073 stays with the ticket actually numbered 073. 0825, 0841,
  0843 likewise. Each says so in its own body.
- **Ticket 0874 (wrong protein transcript) sits at priority 9**, alongside
  0686. It ships a wrong clinical fact today, which outranks the internal
  conversion program at 8. Everything else from the research sits at 7 and
  below so it cannot jump the conversion work.
- **Two March issues were not carried over.** The
  spec-fixture-lifecycle-red-on-main issue is stale — all nine parametrised
  instances of the six tests it names passed on main today. Issue 611 is
  the same defect as ticket 0686 and is folded into that ticket's body.
- **March priorities carried over unchanged.** 0686 arrives ready, not
  failed; its March failure was a paused queue.
- The `triage` skill's priority direction was already corrected by another
  session; only the line wrapping needed tidying.

## Next

1. **Confirm beelink's sync imports this repo with no quarantine line** —
   `~/.local/share/sdlc/cron.log`, or ask the sdlc side. That is the only
   open done-check from the harvester fix.
2. When Ian says go: resume the channel
   (`sdlc resume genomoncology/biomcp`), then watch the first
   flight. 0686 should claim first. Until then the pause holds.
3. Nothing to triage — it is done. New findings should become issues
   again and be triaged deliberately; do not write tickets straight from
   a review.

## Warnings

- **`biomcp` on PATH is a stale build reporting the same version as the
  repo build.** Both say `0.8.25`; the installed one lacks `gene cspec`.
  Test against `./target/release/biomcp`. This already caused two wrong
  issue reports. Ticket 0875 fixes it.
- **Never run `sdlc sync` inside a repo you do not mean to register** —
  registration is a side effect.
- **Run the harvester from `~/workspace/repos/sdlc`**, with an absolute
  `--repo` path. Running it from elsewhere fails after it has already
  cleared the archive; that happened today and cost a re-run.
- Both decisions that were pending are now made and written into tickets.
  Ian chose (2026-08-08) to keep ordinary fulltext `not_included`
  package-only — ticket 0889 corrects the design note, not the code, and
  says so; do not reopen it. The live PubTator call moves to a local
  fixture, not to `make verify` — ticket 0884, priority 9, because it sits
  inside a gate rung.
- `~/workspace/planning/` and `notes/` are Obsidian-synced and gitignored.
  Never `git add -f` them.
- The biomcp-research skill exists twice: `~/workspace/.agents/skills/`
  (the one the workspace loads) and `sdlc/skills/biomcp-research/SKILL.md`
  here, committed by another session. Worth reconciling to one.

## Workspace side (from the operator session, same day)

State the factory computes itself — `factory-status`, `flight-report`,
`factory-wake` in `~/.local/bin` — is not restated here.

- queue 0025 (one flight per channel, channels in parallel, p9) is
  the big structural ticket, behind 0023 (wake) and 0024 (status
  board). If it refuses or blocks, dissect the run first — never
  re-fly blind. After it lands: a small sdlc follow-up enables the
  caps; biomcp then gets its own lane instead of sharing one.
- Decided today: wake/status verbs live in queue's CLI (non-sdlc
  factories inherit them); per-channel parallelism now, second
  machine stays in the direction note; handoff skill carries its
  own disposal rule; priority direction (higher sooner) fixed in
  triage and lead skills.
- Deploy trap: pulling repos/queue or repos/botassembly IS a deploy
  (symlinked dep; ~/.local/bin/bot execs the checkout). Pull
  between flights only.
- Still open workspace-side: march-harvest resolver spike, deck
  repo awaiting a GitHub remote, PreCompact hook offer (not yet
  approved), provider-unavailable break test.

## How the backlog was shaped (2026-08-08)

Sixteen March issues became ten tickets, not sixteen. Three compressions,
each because one-to-one filing would have put several agents in the same
code path:

- **0885 absorbs five live-canary reports** (605, 606, 612, 614, 636).
  They are five sightings of one defect — a canary asserted a public
  service was up, it was not, and the report reads as though BioMCP broke.
  Per-provider fixes cannot converge; the next outage files the sixth.
- **0887 merges both 595 issues** into one behaviour: refuse bad input
  before any provider work.
- **Two were never carried over at all** — the fixture-lifecycle
  red-on-main issue was stale (its six tests pass), and 611 was the same
  defect as 0686 and lives in that ticket's body.

Priority bands as set: 9 gate-blocking and shipped-wrong-data, 8 the
live-assertion conversion program, 7 security and toolchain honesty, 6
input-validation and article-source correctness, 5 gnomAD frequency work,
4 provenance and transparency, 3 coverage and cleanup.
