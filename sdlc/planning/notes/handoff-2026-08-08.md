# Handoff — 2026-08-08

Read, act, then delete this file in your first commit.

Context: BioMCP's migration off the retiring March factory onto sdlc.
Jobs 1 (scripts) and 2 (archive import) are done and pushed. Jobs 3
(live tickets) and 4 (open issues) are the work in front of you.

## State

- **This repo is registered on beelink and is being quarantined at
  every sync.** Registration already happened — do not treat it as
  pending, and note that `~/.local/share/sdlc/repos` on the laptop
  does not list it, so a local registry check is misleading. The
  quarantine cause is in `sdlc/issues/harvester-mispads-and-collides-archive-ids.md`.
- `sdlc/tickets/` is **empty**. Job 3 has not started. The sixteen
  live March tickets are still only in
  `~/workspace/planning/biomcp/tickets/` (Obsidian-synced,
  gitignored, invisible to the factory).
- March's issue store, `~/workspace/planning/biomcp/issues/`, holds
  109 files. Status frontmatter splits them 66 converted, 21 closed,
  **18 open**. Only the open ones move.
- `sdlc/issues/` holds eleven files: ten raised from BioMCP research
  on 2026-08-08, plus the harvester one.
- The March queue is paused permanently and is never resumed.
- Gate ladder run from `$HOME` against main today: `lint` green,
  `test` 308 passed / 0 failed but **not finished**, `spec` never
  started. Re-run before trusting it.

## Just decided

- **Priority direction: higher number runs sooner.** sdlc claims
  `ORDER BY priority DESC`. The `triage` skill in
  `~/workspace/.agents/skills/triage/SKILL.md` says the opposite
  ("blocking issues get low priority numbers") and Ian has said
  plainly it is wrong and must be fixed. Fix the skill before
  triaging anything, or the whole backlog inverts.
- **March's priority numbers carry over unchanged.** They are
  already correct; do not reshuffle.
- Ticket 686 arrives as **ready, not failed**. Its March
  `failed_reason` is `orphaned active ticket: no live runner found`
  — an artefact of the paused queue, not a real defeat.
- 499 (v0.8.26 release) stays a **draft** until a publish flow exists.
- `make verify` is not a gate rung; see `sdlc/planning/verify-lane.md`.

## Next

1. **Fix the `triage` skill's priority direction.** Blocking
   everything else.
2. **Fix the archive id collisions.** Four, not the two the issue
   names: 0025, 0041, 0043, 0073. Two different causes:
   - **0073 is a harvester bug, no renumbering needed.** March id
     `59` has slug `073-repo-cleanup…`; the harvester trusted the
     slug over the id and collided with the real 073. Filing it as
     `0059` invents nothing. The same rule silently misfiled id 48
     to `0060` and id 49 to `0061` — wrong, though they do not
     collide.
   - **0025, 0041, 0043 are genuine March reuse** — same `id` *and*
     same slug prefix, issued twice on the same day. Ian's call
     between renumbering here (the issue proposes original+800) or
     making sdlc tolerate duplicate archive ids. Recommendation on
     record: fix 0073 in the harvester, then do the sdlc-side
     tolerance rather than inventing three numbers.
   - Fix in `~/workspace/repos/sdlc/scripts/march-harvest.py` and
     re-run it; never hand-patch the 557 archive files.
3. **Job 3** — copy (never move) the sixteen live tickets in as
   drafts under `sdlc/tickets/drafts/`, four-digit numbers and
   priorities unchanged.
4. **Job 4** — bring across roughly twelve of the eighteen open
   issues, not all eighteen. See Warnings.
5. Commit the working-tree issue edits listed below.

## Warnings

- **Uncommitted work in the tree right now**: two rewritten issues
  (`feature-clingen-criteria-specifications-as-an-entity.md`,
  `feature-clingen-expert-panel-assertions.md`) and one new one
  (`two-different-builds-both-call-themselves-0-8-25.md`). The two
  rewrites matter — the originals asked for capabilities that
  already exist.
- **`biomcp` on PATH is a stale build reporting the same version as
  the repo build.** `~/.local/bin/biomcp` and
  `./target/release/biomcp` both say `0.8.25` and expose different
  commands; the installed one lacks `gene cspec`. Test against
  `./target/release/biomcp`. This trap already caused two wrong
  issue reports in one sitting.
- **Do not carry all eighteen open March issues.**
  - `2026-07-27-spec-fixture-lifecycle-tests-fail-on-main.md` is
    **stale** — it claims `make test` is red on main and names six
    tests; all nine parametrised instances passed today. Close it.
  - `611-spec-runner-orphans-fixture-processes.md` **duplicates
    ticket 686**. Fold it into 686's body, do not file it twice.
  - Four are live-verify-lane work (605, 606, 612, 614) for a lane
    deliberately outside the factory. Low priority or leave in the
    issue file with a dated line.
  - Two carry `blocks: next-ticket`: `593` (a live PubTator call
    still inside `make test`, so it hits every flight) and `600`
    (needs a design decision from Ian, not code).
- **Never run `sdlc sync` from inside a repo you do not intend to
  register** — registration is a side effect of running it.
- `~/workspace/planning/` and `notes/` are Obsidian-synced and
  gitignored on purpose. Never `git add -f` them.
- The skill written this session lives at
  `~/workspace/.agents/skills/biomcp-research/SKILL.md`; a copy was
  committed here at `sdlc/skills/biomcp-research/SKILL.md` by
  another session. Two copies now exist — worth reconciling.
- Deliverable from today's research, for context on where the ten
  new issues came from: `~/workspace/notes/pten-gn003-research-findings.md`.
