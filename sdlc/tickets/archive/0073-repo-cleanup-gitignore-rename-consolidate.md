---
flow: build
priority: 8
---
# Repo cleanup — gitignore, rename, consolidate

The repo has accumulated build/agent artifacts that shouldn't be in git, stale folders, and a confusing split between `demo/` and `examples/`. Clean it up so the public repo is tidy.

Completed under March on 2026-03-28, as March ticket 59. Imported as history when BioMCP
moved to the sdlc factory; it was never run by this factory.

Work products from the run — design, reviews, dev log, verify:

    /home/ian/workspace/planning/biomcp/artifacts/073-repo-cleanup-gitignore-rename-consolidate
