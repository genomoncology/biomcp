---
flow: build
priority: 7
---
# Refresh source-aware integration architecture contracts

The architecture corpus now disagrees with itself about the entity-to-source map, the strength of `biomcp health`, how universal the shared HTTP-client pattern is, and what proof surfaces are required for source additions. There is also no durable contract for source-aware sections or multi-source ranking, so behavior has become implementation-defined instead of planned.

Completed under March on 2026-03-19, as March ticket 019. Imported as history when BioMCP
moved to the sdlc factory; it was never run by this factory.

Work products from the run — design, reviews, dev log, verify:

    /home/ian/workspace/planning/biomcp/artifacts/019-source-integration-architecture-contracts

The landed commit range could not be recovered from git, so no
record accompanies this entry. That is a known gap for the
earliest tickets, not a sign the work is missing.
