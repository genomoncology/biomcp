---
flow: build
priority: 4
---
# Document BioMCP source-integration architecture and source-addition checklist

The v0.9 expansion tickets currently have to restate the same BioMCP architecture rules over and over: source modules, entity sections, source labels, progressive disclosure, graceful degradation, auth handling, rate limits, and docs updates. That logic belongs in one durable repo document. Without it, each new source ticket drifts slightly and reviewers have to re-assert the same design guidance every time.

Completed under March on 2026-03-19, as March ticket 012. Imported as history when this
repo moved to the sdlc factory; it was never run by this factory.

Work products from the run — design, reviews, dev log, verify:

    /home/ian/workspace/planning/biomcp/artifacts/012-source-integration-architecture-docs

The landed commit range could not be recovered from git, so no
record accompanies this entry. The work products above are the
evidence that survives; the absence of a record is a gap in what
git can still prove, not a sign the work is missing.
