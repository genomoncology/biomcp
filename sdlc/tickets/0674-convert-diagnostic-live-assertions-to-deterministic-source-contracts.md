---
flow: build
priority: 8
deps: ["0651"]
---
# Convert diagnostic live assertions to deterministic source contracts

## Done when

The GTR, WHO IVD, and OpenFDA blocks in spec/entity/diagnostic.md run
routinely against local provider-faithful responses. Provider selection,
regulatory overlays, source attribution, valid empty results, not-configured
sources, failures, and rendered output are deterministic.

## Proof required

The source modules already have broad planning and decoder tests. Keep them
and add only missing receipt, consumed-plan/orchestration, local executor,
CLI-process, and renderer proof. Captures must be dated, receipted, and
generated by the production request path.

Do not change provider selection, nested-output compactness, health behavior,
or licensing in this conversion.

The design stage authors replacement assertions. diagnostic.md becomes
routine; any live remainder moves verbatim to diagnostic-live.md. Registry
arrays, Makefile, and architecture inventory agree.

## Authorized test changes

Design commits may restate the source-backed diagnostic blocks, GTR/WHO
IVD/OpenFDA fixture routes and receipts, source/entity tests needed for the
missing layers, and registry entries. Mechanical construction fixes may land
with implementation without changing assertions.

The src line ceiling may rise by at most 140 lines.
