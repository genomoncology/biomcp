---
flow: quickfix
priority: 10
---

# Restore the opt-in verification lane

`make verify` is deterministically red before it can give an honest live-provider verdict: MCP client assertions still expect compacted wording in the raw tool description, the CLI lane contract contradicts its own registry, the variant strict-query canary assumes only one normalized alias, the credentialed UMLS check runs through a wrapper that removes its credential, and the NIH Reporter mode runs a fixture-dependent routine page without its fixtures. Align these checks with the shipped contracts while preserving the routine/live split and fail-closed product errors.

The MCP client must assert the current compact raw-tool policy rather than restoring stale production prose. The contract must retain explicit checks for read-only behavior, preference for typed tools, discovery commands, JSON guidance, and rejection of binary or mutating commands. Both ignored stdio and HTTP contracts must exercise the same current split between the compact raw-tool description and server instructions.

Remove only the contradictory assertion that `spec/surface/cli.md` is absent from the live registry; keep the positive registry and routine/live separation checks. The variant-article canary must validate every emitted strict-query row rather than collapsing rows by provider or accepting one matching row. Every row must use one of the four exact provider templates, keep its gene attached to its own `query_alias`, and derive its query exactly from that alias. All four providers must exist for every emitted alias, both original BRCA1 collision aliases must remain present and distinct, and the existing discovery, executed-route, and provenance checks remain hard failures. Tests must include multiple aliases and malformed or extra rows.

`tools/biomcp-ci` must continue stripping every credential by default. Add one narrow explicit opt-in that preserves only `UMLS_API_KEY`, used only by the credentialed UMLS block in `spec/surface/discover-live.md`; it must not preserve any other key or print the credential value. A missing UMLS credential must fail before provider work with a clear setup error rather than silently reducing the check to OLS-only behavior.

Add `spec/entity/nih-reporter-live.md` and make `verify-nih-reporter` run only that page without routine provider fixtures. It must issue one bounded live funding request and require a non-empty, correctly shaped funding result. Tighten `tools/biomcp-verify-live` so only the exact known NIH Reporter provider-unavailable outcome becomes operator-pending. Malformed JSON, a successful response missing required funding fields or its table, command errors, and unknown failures must remain product-red.

The complete authorized implementation and proof set is `crates/biomcp-mcp-contract-client/src/lib.rs`, `scripts/run-specs.sh`, `tools/biomcp-ci`, `tools/biomcp-verify-live`, `spec/surface/cli.md`, `spec/surface/discover-live.md`, `spec/entity/variant-articles-live.md`, `spec/entity/nih-reporter-live.md`, `spec/fixtures/run-variant-article-strict-live-canary.sh`, `spec/README-timings.md`, `tests/test_variant_article_live_canary.py`, `tests/test_biomcp_ci_contract.py`, `tests/surface/test_biomcp_verify_live.py`, and `tests/surface/test_parallel_isolation_contract.py`. Existing related assertions in those exact files may be restated, but routine fixtures, production provider behavior, and the shipped MCP catalog are not authorized to change.

Done means deterministic structure and fixture tests prove credential stripping and UMLS-only preservation, every-row variant-query validation, live-registry completeness and disjointness, the dedicated NIH page and absence of routine fixture startup, and narrow fail-closed live classification. The two ignored MCP contracts must pass. The corrected non-credentialed live checks must run through the release binary; credentialed UMLS may report a clear setup requirement when no key is available, and genuinely unavailable public providers may retain only their explicitly documented operator-pending outcome.
