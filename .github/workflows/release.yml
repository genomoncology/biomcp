name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-disabled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify canonical installer copy
        run: cmp --silent install.sh docs/install.sh
      - run: bash scripts/release-disabled.sh
