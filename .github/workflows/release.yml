name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-disabled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/release-disabled.sh
